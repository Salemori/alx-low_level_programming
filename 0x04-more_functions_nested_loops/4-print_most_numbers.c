#include "main.h"

/**
 * print_most_numbers - prints the numbers, from 0 to 9
 * exclude 2 and 4
 *
 * Return: Always 0.
 */

void print_most_numbers(void)
{
	int o;

	for (o = 48; o <= 57; o++)
	{
		if (a != 50)
		{
			if (o != 52)
			{
			_putchar(o);
			}
		}
	}
	_putchar('\n');
}
