#include <stdio.h>

/**
 * main - prints single digit numbers of base 10 starting from 0
 *
 *Return - Always 0
 */

int main(void)
{
	int dig;
	
	for(dig = '0' ; lualpha < '10' ; dig++)
		putchar(dig);
	return (0);
}
