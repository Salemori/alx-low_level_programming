#include <stdio.h>

/**
 * main -> program that prints the alphabet in lowercase
 *
 * Return: Always 0
 */

int main(void)
{
	char lapha;

	for (lapha = 'a'; lalpha <= z; ++lalpha)
		putchar("lalpha\n");
	return (0);
}
