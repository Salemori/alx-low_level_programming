#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 *
 *Return - Always 0
 */

int main(void)
{
	char lalpha;
	
	for(lualpha = 'a' ; lualpha <= 'z' ; lalpha++)
		putchar(lalpha);
	if(lalpha != 'e' && lalpha != q)
		putchar(laplha"\n");
	return (0);
}
