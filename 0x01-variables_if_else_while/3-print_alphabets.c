nclude <studio.h>
/**
 *main - prints the alphabet in lowercase, and then in uppercase
 *
 *Return - Always 0
 */

int main(void)
{
	char luapha;

	for(lualpha = 'a' ; lualpha <= 'z' ; ++lualpha)
		putchar(lualpha);
	for(lualpha = 'A' ; lualpha <= 'Z' ; lualpha++)
		putchar(lualpha);
	return (0);
}
