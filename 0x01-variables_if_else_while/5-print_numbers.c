#include <stdio.h>
/**
 * main -> prints single digit numbers of base 10
 *
 * Return: Always 0
 */

int main(void)
{
	int digt;

	for (digt = 0 ; digt < 10 ; digt++)
		printf("%d", digt);
	printf("\n");
	return (0);
}

